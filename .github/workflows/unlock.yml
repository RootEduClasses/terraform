name: unlock

on:
  workflow_dispatch:
    inputs:
      release_version:
        description: Release version to be locked
        required: false

jobs:
  lock-or-unlock:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v3
    - name: Run Branch Protection
      env:
        GH_TOKEN: ${{ secrets.GH_KEY }}
        REL: ${{github.event.inputs.release_version}}
      run: |
        for i in `cat repos`
        do
        for j in `cat rel-versions`
        do
        curl -L \
        -X PUT \
        -H "Accept: application/vnd.github+json" \
        -H "Authorization: Bearer $GH_TOKEN" \
        -H "X-GitHub-Api-Version: 2022-11-28" \
        https://api.github.com/repos/RootEduClasses/$i/branches/release/platform-$j/protection \
        -d '{ "required_status_checks": 
        {
        "strict": true,
        "contexts": [
        "continuous-integration/travis-ci"
        ]
        },
        "enforce_admins": true,
        "required_pull_request_reviews": {
        "dismissal_restrictions": {
        "users": [
        "octocat"
        ],
        "teams": [
        ""
        ]
        },
        "dismiss_stale_reviews": true,
        "require_code_owner_reviews": true,
        "required_approving_review_count": 2,
        "require_last_push_approval": true,
        "bypass_pull_request_allowances": {
        "users": [
        ""
        ],
        "teams": [
        ""
        ]
        }
        },
        "restrictions": {
        "users": [""],
        "teams": [""],
        "apps": [""]
        },
        "required_linear_history": true,
        "allow_force_pushes": true,
        "allow_deletions": true,
        "block_creations": true,
        "required_conversation_resolution": true,
        "lock_branch": false,
        "allow_fork_syncing": true
        }'
        done
        done
